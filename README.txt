This is a basic library that has 2 functions:

isPrime -  A function for checking if a number is prime.
primeNum - A function that receives a number (2<X<100) and return a list that is containing all the prime numbers that are smaller then X.